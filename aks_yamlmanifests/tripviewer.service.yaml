apiVersion: v1
kind: Service
metadata:
  name: tripviewer-service
spec:
  type: LoadBalancer
  selector:
    tier: tripviewer
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 80
